name: Visual Review

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    uses: Carenetic-Digital/cf-workers-review-tool/.github/workflows/cf-review-reusable.yml@main
    secrets: inherit
